# HANDOFF_LUXE-SITE

**プロジェクトタグ: `LUXE-SITE`** ／ LUXE PARTY TOKYO 公式サイト
（関連する別プロジェクトのハンドオフと混同しないこと。本ファイルはサイト本体のみを対象とし、
イベント登録システム・動画パイプライン・TOKOWAKA関連は範囲外）

このリポジトリを Claude Code で引き継ぐための資料です。
**まず本ファイルと `README.md` を読んでから着手してください。**

---

## 0. 30秒サマリ

Googleスプレッドシート＋Driveを入力、GAS（Apps Script）をビルダー、GitHub Pagesを配信先とする
静的サイト。ナイトイベント（LUXE PARTY TOKYO）の公式サイトで、開催予定イベントがトップ、
終演済みは自動でアーカイブに落ちる。**ビルドツール・サーバー・課金サービスは一切使わない**のが設計上の縛り。

現状は「日本語・単一言語・ローカル検証済み」まで完了。
次の作業は **英語対応（/en/）** と **公開前タスク**。

---

## 1. 現状

### 完成しているもの
- 静的サイト生成の全ロジック（`_shared/render.js`）
- GAS本体（`gas/Code.gs`）— シート読取、検証、Drive画像取得、GitHubへ1コミット反映
- テンプレート2種、共通CSS、法務3ページ（内容は雛形）
- ローカル生成スクリプト（`tools/build-local.js`）— GASと同じrender.jsを使う
- サンプルデータ3イベント分＋ダミー画像（そのままブラウザで動作確認できる）

### 検証済み（Headless Chromiumで実施）
- 全ページ巡回でリンク切れ0件
- モバイル360px／デスクトップ1440pxで横スクロールなし
- `published` を外したブロックが出力されないこと
- 過去イベントで ENTRY ボタンが消え ENDED バッジが付くこと
- 深夜またぎ判定（22:00開演・翌05:00終演のイベントが、翌3時時点で「開催中」・翌6時で「終了」）
- render.js が GAS 相当のグローバル読み込みでも動くこと

### 未着手
- 英語対応、公開前タスク一式（§5参照）
- 実運用でのGAS実行（GitHubトークン未設定のため未実行）

---

## 2. ファイルマップ

```
_shared/render.js        ★生成ロジックの唯一の実体。GAS側は同内容を render.gs として配置
_templates/event.html    イベントページの骨格（{{PLACEHOLDER}} 方式）
_templates/archive.html  アーカイブ（年ごとの壁）の骨格
assets/site.css          全ページ共通CSS。色・字送りは :root にまとまっている
assets/<eventId>/*.jpg   GASが生成する画像（ファイル名にコンテンツハッシュ付き）
gas/Code.gs              Apps Script 本体
tools/build-local.js     ローカル生成（検証用＋GAS障害時の非常口）
sample/sheet-sample.json シートを模したテストデータ
data/site.json           生成物。全データのバックアップを兼ねる
data/manifest.json       画像の差分判定用（GASが管理。手で触らない）
index.html / archive/ / events/<id>/   生成物。手で編集しない
attention.html / privacy.html / tokusho.html   法務ページ（手で編集する）
```

**生成物を直接編集しないこと。** 次回の「サイトを更新」で上書きされます。

---

## 3. 守るべき不変条件

作業中にこれらを壊さないでください。壊すと運用事故になります。

1. **render.js は Node と GAS の両方で動く純粋関数のみ。** `require` / `fs` / `DriveApp` / `SpreadsheetApp` を書かない。ES2017まで。
2. **render.js と render.gs は同一内容。** 片方だけ直さない。
3. **画像ファイル名にはコンテンツハッシュを付ける。** キャッシュで古い画像が残る事故を防いでいる。
4. **GitHubへの反映は Git Data API で1コミット。** ファイルごとの contents API に戻さない（途中失敗で中途半端に公開される）。
5. **アーカイブ判定は終演時刻ベース・Asia/Tokyo固定。** 日付比較に戻さない（深夜イベントが開催中にアーカイブ入りする）。
6. **未知の block type は無視する。** 例外を投げない（シートの打ち間違いでサイト全体が落ちる）。
7. **公開前に validate_() で止める。** 検証をスキップする分岐を追加しない。
8. **動画をリポジトリに入れない。** GitHub Pages の容量・帯域上限に触れる。

---

## 4. 確定した設計判断（と却下案）

| 判断 | 理由 |
|---|---|
| microCMS を使わない | 費用と管理画面依存を避ける。シート＋Driveで足りる |
| ビルドツールを使わない | GitHub Pages に直置きできる状態を保つ。CI依存を作らない |
| GitHub Actions での画像圧縮を**却下** | GASとActionsの二重書き込みで manifest.json が壊れる。Google側の縮小（`imageMode: thumbnail`）で十分 |
| 画像縮小は Drive のサムネイル経由 | 無料・EXIF回転が適用済み・事前書き出し不要。失敗時は原本にフォールバック |
| アーカイブは自動判定 | 手動フラグは必ず更新漏れが起きる |
| 英語は `/en/` 配下に別ページ生成 | JS切替はSEOとOGPが機能しない |

---

## 5. 次のタスク（優先順・受け入れ条件つき）

### T1. 英語対応 【最優先・構造に関わる】
- `events` / `blocks` シートに `title_en` `lead_en` `body_en` 列を追加
- `/en/`, `/en/archive/`, `/en/events/<id>/` を生成
- 両言語のページに相互の `<link rel="alternate" hreflang>` を出力
- ヘッダーに JA / EN 切替を追加（対応ページが無ければトップへ）
- 英語列が空の場合は日本語にフォールバックし、**エラーにしない**

*受け入れ条件*: `node tools/build-local.js` で `/en/` が生成され、リンク切れ0件、hreflang が双方向に張られている。

### T2. GASに翻訳ボタンを追加
- メニュー「サイト」に「英語列を生成」を追加
- Claude API（キーはスクリプトプロパティ `ANTHROPIC_API_KEY`）で日本語列→英語列を埋める
- **既に値が入っている英語列は上書きしない**（手直しが消える事故を防ぐ）
- 固有名詞（イベント名・会場名）は config の対訳表を優先

*受け入れ条件*: 空欄のみ埋まる。API失敗時はシートを壊さず中止し、ログタブに記録。

### T3. チケット導線の2系統化
- `events` に `ticketUrl`（外部チケットサービス）を追加。既存 `entryUrl` は無料エントリー／ゲストリスト用
- ヒーローとENTRYブロックで「TICKET」「GUEST LIST」を出し分け
- 両方空ならボタンを出さない

### T4. 公開前タスク
- OGP画像テンプレート（1200×630）
- favicon / apple-touch-icon
- `404.html`
- `sitemap.xml` と `robots.txt` の生成（GAS側）
- 解析タグの埋め込み口を config 化（GA4測定ID・MetaピクセルID）
- 法務3ページに実内容を反映（**内容は依頼者から受領が必要**）

### T5. ドメイン切替
- `CNAME` ファイル追加＋DNS設定
- **既存のイベント登録システムと同一ドメインのため、配置の決定が前提**（§6）

---

## 6. 未確定・依頼者の回答待ち（着手前に確認）

1. **ドメインと既存登録システムの配置** — 新サイトをドメイン直下に置くと、稼働中の登録システムのURLが死ぬ。過去に配布したQR・DMのリンクも切れる。推奨は登録システムを `entry.<domain>` に分離する案だが未決。**T5はこの回答が出るまで着手しない。**
2. **外部チケットサービスの名称** — 導線とボタン文言が変わる
3. **特商法の記載内容** — 事業者名・所在地・連絡先・キャンセル規定
4. **実写素材** — ヒーロー、OGP、ギャラリー
5. **英語の正式表記** — イベント名・会場名

---

## 7. 開発と検証の手順

```bash
# 生成
node tools/build-local.js

# ローカル確認
python3 -m http.server 8000    # → http://localhost:8000

# 構文チェック
node -e "require('./_shared/render.js')" && node --check tools/build-local.js
```

変更を入れたら、最低限これを毎回確認してください。

- 全ページを巡回してリンク切れ0件
- 幅360pxで横スクロールが出ない
- 開催予定イベント：ENTERボタンあり／ENDEDバッジなし
- 過去イベント：ENTERボタンなし／ENDEDバッジあり
- `published` を外した行が出力されない
- 深夜またぎのイベントが開催中にアーカイブへ落ちない

Headless Chromium（Playwright）が使える環境なら自動化して構いません。

---

## 8. 落とし穴

- **GAS側の反映漏れ**: `_shared/render.js` を直しても、Apps Script の `render.gs` に貼り直さないと本番は変わらない。ここが最も事故りやすい
- **GitHubトークンの期限切れ**: 突然すべて失敗する。まず疑うのはここ
- **Driveのフォルダ名**: イベントIDと完全一致していないと画像が出ない（エラーにはならず、単に出ない）
- **`_` で始まるディレクトリ**: GitHub Pages の Jekyll に無視される可能性があるため `.nojekyll` を置いてある。消さないこと
- **Googleフォント**: 制限された環境ではフォント読み込みが失敗しフォールバック表示になる。デザイン崩れではない

---

## 9. 依頼者について

- 判断が速く、方針は自分で決めるので、選択肢と根拠を出せば十分
- 「敵対的レビュー」を求められる。実装前に失敗経路を洗い出して提示すること
- 運用リソースの最小化と無料での実現が一貫した要求。有料サービスやCI依存を提案する場合は、それが必要な理由を明示する
